# Bankr API key (recommended: Bankr-only launches, no RPC needed)
# Get from bankr.bot/api. For /deploy: key must have Agent API (write) access enabled.
BANKR_API_KEY=bk_S5B47PUYHJUAHKRDX3DG4ZFLEV7RTU5Q

# Base RPC URL (for chain fallback when indexer fails)
RPC_URL_BASE=https://base-mainnet.g.alchemy.com/v2/YOUR_KEY

# Doppler indexer (GraphQL at /graphql; fees: pools by baseToken, then cumulatedFees(poolId, chainId, beneficiary))
# Base mainnet: https://indexer.doppler.lol is often 502 — for /fees use your own (e.g. doppler-indexer on Railway).
DOPPLER_INDEXER_URL=https://testnet-indexer.doppler.lol
CHAIN_ID=8453

# Discord: two channels for what you want
# 1) Real-time deployments (all new Bankr tokens) — always on
DISCORD_ALERT_CHANNEL_ID=1234567890123456789

# 2) Watch list only (wallet/X/Farcaster/keyword matches) — elsewhere
DISCORD_WATCH_ALERT_CHANNEL_ID=9876543210987654321

# Or use webhook (only when neither DISCORD_ALERT_CHANNEL_ID nor DISCORD_WATCH_ALERT_CHANNEL_ID is set)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Telegram: message @BotFather to create bot, get token. Chat ID from @userinfobot
TELEGRAM_BOT_TOKEN=123456:ABC...
TELEGRAM_CHAT_ID=-1001234567890

# Notify loop: ms between polls (default 60000 = 1 min; use 30000 for 30 sec)
# POLL_INTERVAL_MS=60000

# Watch list: only notify when deployer matches (overrides FILTER_X_MATCH)
# WATCH_X_USERS=thryxagi,crewdegen
# WATCH_FC_USERS=dwr.eth,vitalik.eth
# WATCH_WALLETS=0x1234...,0x5678...
# WATCH_KEYWORDS=pepe,btc,sol,"this is a test"

# Optional: path for seen-tokens persistence (use a volume path so it survives deploys)
# SEEN_FILE=/data/bankr-seen.json

# Optional: cap seen list size (e.g. 3000) to avoid unbounded growth; if unset, seen list is unbounded and tokens are never re-pinged.
# SEEN_MAX_KEYS=3000
